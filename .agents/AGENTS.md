# Project Directives

- **Chrome Browser Mode**: Always launch Chrome in visible / preview mode (`headless=False`) for all automation tasks, scripts, and tests so that the user can observe live browser execution.
